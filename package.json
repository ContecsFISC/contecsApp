{
  "name": "contecs-app",
  "description": "Aplicación web y configuración de CONTECS",
  "scripts": {
    "lint": "npm --prefix functions run lint",
    "emulators": "firebase emulators:start",
    "deploy": "firebase deploy --only functions,hosting,firestore,storage"
  },
  "private": true,
  "type": "module"
}
